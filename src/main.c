#include <stdio.h>
#include "project.h"

int main(int argc, char **argv) {
    printf("Proyecto SO - versión: %s\n", project_version());
    printf("(Funcionalidad principal pendiente de implementar según el enunciado)\n");
    return 0;
}
